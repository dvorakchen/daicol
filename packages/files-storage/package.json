{
  "name": "@daicol/files-storage",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "minio": "^8.0.6",
    "sharp": "^0.34.4",
    "uuid": "^13.0.0"
  },
  "scripts": {
    "test": "deno test --allow-env --allow-ffi --allow-read",
    "format": "prettier --write .",
    "lint": "prettier --check . && eslint ."
  }
}
