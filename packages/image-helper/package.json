{
  "name": "@daicol/image-helper",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "jimp": "^1.6.0"
  },
  "scripts": {
    "test": "deno test --allow-env --allow-ffi --allow-read",
    "format": "prettier --write .",
    "lint": "prettier --check . && eslint ."
  }
}
